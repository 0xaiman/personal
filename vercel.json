{
    "version": 2,
    "builds": [
      {
        "src": "src/**/*.tsx",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/src/$1"
      }
    ],
    "scripts": {
      "build": "vite build",
      "install": "npm install"
    }
  }
  